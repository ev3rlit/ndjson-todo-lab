services:
  lb:
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ${NGINX_LOG_DIR:-/srv/ndjson-todo/logs/nginx}:/var/log/nginx

  web1:
    volumes:
      - ${TODO_DATA_DIR:-/srv/ndjson-todo/data}:/workspace/data
      - ${TODO_LOG_DIR:-/srv/ndjson-todo/logs}:/workspace/logs

  web2:
    volumes:
      - ${TODO_DATA_DIR:-/srv/ndjson-todo/data}:/workspace/data
      - ${TODO_LOG_DIR:-/srv/ndjson-todo/logs}:/workspace/logs

  web3:
    volumes:
      - ${TODO_DATA_DIR:-/srv/ndjson-todo/data}:/workspace/data
      - ${TODO_LOG_DIR:-/srv/ndjson-todo/logs}:/workspace/logs
